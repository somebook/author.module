= form_for [:author, :album, @photo], html: { class: %w(form-horizontal) }, multipart: true do |photo_form|
  %fieldset
    %legend= @form_legend
    = present @photo, :photo_form_presenter, photo_form do |p|
      = p.errors
      = p.title
      = p.summary
      = p.file
      = p.album_id @album.id
      = p.actions back: author_album_photos_path(@album)